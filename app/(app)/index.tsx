import HomeScreen from '../../src/screens/HomeScreen';

// This file defines the home page route for the main app group.
// It tells the router to display the HomeScreen component.
export default function AppHomeRoute() {
  return <HomeScreen />;
}
